name: Feature branch CI

on:
  push:
    pull_request:
      branches:
        - 'master'
jobs:
  test:
    name: Tests
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - run: sh ./gradlew test