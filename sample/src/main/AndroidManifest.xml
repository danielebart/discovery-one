<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.discoveryone.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name=".activity2.Activity2"
            android:theme="@style/Theme.App.NoActionBar">
        </activity>
        <activity
            android:name=".activity3.Activity3"
            android:theme="@style/Theme.App.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity4.Activity4"
            android:theme="@style/Theme.App.NoActionBar" />
        <activity
            android:name=".activity1.Activity1"
            android:label="@string/app_name"
            android:theme="@style/Theme.App.NoActionBar"></activity>
    </application>

</manifest>